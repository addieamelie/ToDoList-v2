<!DOCTYPE html>
    <head>
        <meta charset="utf-8">
        <title>To Do List</title>
    </head>
    <body>
        <h1> <%= kindOfDay %> </h1>
        <ul>
            <li>Buy Food</li>
            <li>Cook Food</li>
            <li>Eat Food</li>

           <% for (var i = 0; i<newListItems.length; i++) { %>
                <li><%= newListItems[i] %></li>
           <% } %>
            
        </ul>

        <form action="/" method="POST">
        <input type="text" name="newItem" placeholder="New list item">
        <button type="submit">Add</button>    
        </form>
            </body>
</html>