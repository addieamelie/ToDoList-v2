<!DOCTYPE html>
    <head>
        <meta charset="utf-8">
        <title>To Do List</title>
    </head>
    <body>
       <% if (kindOfDay ==="Tuesday" || kindOfDay === "Sunday") { %>
            <h1 style="color: #ff9999"><%= kindOfDay%> List</h1>
       <% } else { %>
            <h1 style="color: #ffc8c8"><%= kindOfDay%> List</h1>
      <%  } %>
        
            </body>
</html>